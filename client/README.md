# Teable Sync Web Client

Frontend workspace for Teable Sync.

```bash
npm install
npm run dev
```

By default the Vite dev server listens on `0.0.0.0:5173` and proxies API and WebSocket traffic to the backend on `localhost:3101`.
